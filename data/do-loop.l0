fun [int] main() = 
    let N = 10 in //read(int) in
    let x = iota(N)   in
    let x = 
        for i < N-1 do
            let x[i+1] = x[i+1] + x[i] 
            in  x
        merge x
    in x
        
