fun int tupfun( (int*(real*int)) x, (int*(real*int)) y ) = 
    let (x1, x2) = x in
    let (y1, y2) = y in
        x1 + y1
    //let (x0, (x1,x2)) = x in
    //let (y0, (y1,y2)) = y in
    //33

fun int main() = 
    let x = ( read(int), ( read(real), read(int) ) ) in
    let y = ( read(int)*2, ( read(real)+4.0, read(int)+1 ) ) in
    write(tupfun(x,y))
