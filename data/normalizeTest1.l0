fun int tupfun( (int,(real,int)) x, (int,(real,int)) y ) = 
    let (x1, x2) = x in
    let (y1, y2) = y in
        x1 + y1
    //let (x0, (x1,x2)) = x in
    //let (y0, (y1,y2)) = y in
    //33

fun int main((int, (real, int)) x, (int, (real, int)) y) = 
    tupfun(x,y)
